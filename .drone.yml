name: drone-demo

kind: pipeline
type: docker
steps:
  - name: backend
    image: golang:alpine
      - pwd
      - ls
      - CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -o app .
      - ./app